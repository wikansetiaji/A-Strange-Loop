import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Phase 1 MVP: Firebase-dependent tests will come in later phases.
    expect(true, isTrue);
  });
}
